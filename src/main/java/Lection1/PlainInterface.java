package Lection1;

@FunctionalInterface// нужен для определения функионального интефейса PS ставить не оябзательно но зато если мы зпходим
        //добавить второй метод будет ошибка
public interface PlainInterface {
    String action(int x, int y);
}
